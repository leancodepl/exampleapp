import { attribute } from "../../utils/functions/attribute"

export const dataLabelInHeader = attribute<"">("label-in-header")
export const dataSize = attribute<"large" | "normal">("size")
export const dataError = attribute<"">("error")
